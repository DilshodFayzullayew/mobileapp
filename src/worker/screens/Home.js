import React from 'react';
import { View, Text, StyleSheet, Image, FlatList, TouchableOpacity } from 'react-native';
import NewsHome from '../../mainComponent/NewsHome'
import NewsHelper from '../../mainComponent/NewsHelper'
const Home = () => {
  
  return (
   <View>

     <NewsHome/>
   </View>
  );
};

const styles = StyleSheet.create({
 
});

export default Home;
