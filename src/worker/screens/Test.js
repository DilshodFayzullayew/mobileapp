import React from 'react'
import { View, Text, Image, StyleSheet, TouchableOpacity, FlatList, ScrollView } from 'react-native'
import TestComponent from '../../mainComponent/TestComponent'
export default Test = () => {
  
  return (
    <TestComponent />
  )
}

const styles = StyleSheet.create({
  
})